
public – Accessible from anywhere.

private – Accessible only within the same class.

protected – Accessible within package and subclasses.

class – Defines a class.

interface – Defines an interface.

extends – Allows a class to inherit from another class.

implements – Allows a class to use an interface.

new – Creates a new object.

this – Refers to the current object.

super – Refers to the parent class object.

boolean – Stores true or false.

byte – Small integer data type.

char – Stores a single character.

double – Stores decimal numbers (double precision).

float – Stores decimal numbers (single precision).

int – Stores integers.

long – Stores large integers.

short – Stores smaller integers.

void – Indicates no return value.

if – Executes code if condition is true.

else – Executes code if condition is false.

switch – Selects one of many code blocks.

case – Defines a block inside switch.

default – Runs if no switch case matches.

while – Repeats code while condition is true.

do – Executes loop at least once.

for – Loop with initialization, condition, and update.

break – Exits a loop or switch.

continue – Skips current loop iteration.

return – Exits a method and optionally returns value.

try – Defines block to test for errors.

catch – Handles exceptions.

finally – Runs code after try/catch.

throw – Throws an exception manually.

throws – Declares exceptions a method may throw.

final – Prevents value, method, or class from changing.

static – Belongs to class rather than objects.

abstract – Declares incomplete class or method.

synchronized – Controls thread access.

volatile – Ensures variable is updated across threads.

transient – Prevents variable from being saved during serialization.

native – Method implemented in another language.

strictfp – Restricts floating-point calculations for consistency.

package – Defines a namespace.

import – Allows use of other packages/classes.

instanceof – Checks if object belongs to a class.

assert – Tests assumptions during debugging.

enum – Defines a fixed set of constants.

const – Reserved but not used in Java.

goto – Reserved but not used in Java.